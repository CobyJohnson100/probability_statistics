# Developer Agent Documentation

**Date Established:** 2025-11-08  

## Helper Statement for AI Agents and Developers

When working on this project, assume you are operating in a WSL or Linux terminal environment. The project is located in `/opt/probability_statistics`, which is a system directory. Therefore, any commands that create, modify, or delete files/directories may require elevated privileges. Use `sudo` for such operations to avoid permission errors.